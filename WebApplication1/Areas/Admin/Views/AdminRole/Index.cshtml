@using EntityLayer.Concrete
@model List<AppRole>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<html>
<body>
    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox ">
                    <div class="ibox-title">
                        <h5>Roles</h5>
                        <div class="ibox-tools">
                            <a class="collapse-link">
                                <i class="fa fa-chevron-up"></i>
                            </a>
                            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                                <i class="fa fa-wrench"></i>
                            </a>
                            <a class="close-link">
                                <i class="fa fa-times"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">

                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Role</th>
                                    <th>Delete</th>
                                    <th>Update</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {

                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.Name</td>
                                        <td><a href="/Admin/AdminRole/DeleteRole?=@item.Id" class="btn btn-danger">Delete</a> </td>
                                        <td><a href="/Admin/AdminRole/UpdateRole?=@item.Id" class="btn btn-success">Update</a> </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/Admin/AdminRole/AddRole/" class="btn btn-outline-primary">Add New Role</a>
                        <a href="/Admin/AdminRole/UserRoleList/" class="btn btn-outline-primary">User Role List</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>

